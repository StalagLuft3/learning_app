const divContainer = {
  display: "grid",
  aligned: "full-width",
  gridTemplateColumns: "85% 14%",
  columnGap: `var(--ic-space-xs)`,
  padding: `var(--ic-space-xs)`,
}

const sectionContainer = {
  display: "inline-block",
  width: "100%",
  padding: `var(--ic-space-xs)`
}

const cardContainer = {
  display: "inline-block",
  width: "100%",
  padding: `var(--ic-space-xs)`
};

  const buttonStyle = {
    margin: "4px",
    padding: "4px"
  };

  export {divContainer, sectionContainer, cardContainer, buttonStyle};
